# Asymmetric Decryption Key (RSA 2048)

Creates an asymmetric decryption key using the `RSA_DECRYPT_OAEP_2048_SHA256` algorithm for envelope encryption scenarios.

## Usage

```bash
terraform init -backend=false
terraform validate
```
