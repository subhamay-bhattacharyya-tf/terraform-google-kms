# Asymmetric Signing Key (EC P-256)

Creates an asymmetric signing key using the `EC_SIGN_P256_SHA256` algorithm. Suitable for digital signatures.

## Usage

```bash
terraform init -backend=false
terraform validate
```
