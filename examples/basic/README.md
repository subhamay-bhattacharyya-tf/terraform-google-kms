# Basic KMS Crypto Key

Creates a minimal symmetric encryption key (`ENCRYPT_DECRYPT`, `SOFTWARE` protection) using all defaults.

## Usage

```bash
terraform init -backend=false
terraform validate
```
