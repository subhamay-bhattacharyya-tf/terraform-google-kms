# Complete KMS Crypto Key

Demonstrates all available features: HSM protection, 90-day rotation, resource labels, and a custom destroy schedule.

## Usage

```bash
terraform init -backend=false
terraform validate
```
