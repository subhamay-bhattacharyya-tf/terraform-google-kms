# HSM-Protected Key

Creates a symmetric encryption key backed by a hardware security module (`HSM` protection level) with 90-day rotation.

## Usage

```bash
terraform init -backend=false
terraform validate
```
