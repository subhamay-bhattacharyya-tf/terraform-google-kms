# HMAC Signing Key

Creates an HMAC signing key using the `HMAC_SHA256` algorithm. Suitable for message authentication codes and webhook signature verification.

## Usage

```bash
terraform init -backend=false
terraform validate
```
