# KMS Crypto Key with Annual Rotation

Creates a symmetric encryption key with automatic key rotation every 365 days (`31536000s`).

## Usage

```bash
terraform init -backend=false
terraform validate
```
