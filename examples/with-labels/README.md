# KMS Crypto Key with Labels

Creates a symmetric encryption key with GCP resource labels for cost attribution and environment tracking.

## Usage

```bash
terraform init -backend=false
terraform validate
```
