# KMS Crypto Key with 90-Day Rotation

Creates a symmetric encryption key with automatic key rotation every 90 days (`7776000s`).

## Usage

```bash
terraform init -backend=false
terraform validate
```
